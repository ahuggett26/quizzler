# Quiz app

Structure:
- packages/shared: shared, general TypeScript files
- packages/frontend: React + Vite + TypeScript
- packages/backend: Express + TypeScript

Quick start:
1. npm install
2. npm run dev   # starts Vite dev server & express backend concurrently
